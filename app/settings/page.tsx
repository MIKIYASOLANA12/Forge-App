"use client";

import Link from "next/link";
import { AlertTriangle, CalendarDays, LoaderCircle, Save, ShieldAlert } from "lucide-react";
import { useEffect, useState } from "react";

type Settings = { examDate: string; planStartDate: string; totalXp: number; level: number; targetCalories: number; targetProtein: number; targetCarbs: number; targetFat: number };
const dateInput = (value: string) => new Date(value).toISOString().slice(0, 10);

export default function SettingsPage() {
  const [settings, setSettings] = useState<Settings | null>(null);
  const [examDate, setExamDate] = useState("");
  const [editing, setEditing] = useState(false);
  const [confirmation, setConfirmation] = useState("");
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState("");
  useEffect(() => { fetch("/api/settings").then((response) => response.json()).then((data: Settings) => { setSettings(data); setExamDate(dateInput(data.examDate)); }); }, []);
  const saveExamDate = async () => { setSaving(true); const response = await fetch("/api/settings", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ examDate }) }); if (response.ok) { setSettings(await response.json()); setEditing(false); setMessage("Exam date saved"); window.setTimeout(() => setMessage(""), 2200); } setSaving(false); };
  const reset = async () => { if (confirmation !== "RESET") return; setSaving(true); const response = await fetch("/api/settings/reset", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ confirmation }) }); setMessage(response.ok ? "All logged progress was reset" : "Reset failed"); setConfirmation(""); setSaving(false); };
  if (!settings) return <div className="flex items-center gap-2 text-sm text-[var(--text-muted)]"><LoaderCircle size={16} className="animate-spin" /> Loading settings...</div>;
  const elapsed = Math.max(0, Math.floor((Date.now() - new Date(settings.planStartDate).getTime()) / 86400000));
  return <div className="mx-auto w-full max-w-[1000px] animate-fade-in pb-10"><section className="mb-8 border-b border-[var(--border)] pb-6"><p className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-[var(--text-secondary)]">Settings / control room</p><h1>Keep the system honest.</h1><p className="mt-2 max-w-xl text-sm">Update the few inputs that shape your priorities. Everything else stays earned.</p></section><div className="space-y-5"><section className="card"><div className="flex items-start justify-between gap-4"><div><h2 className="text-lg">Exam date</h2><p className="mt-1 text-sm">{new Date(settings.examDate).toLocaleDateString(undefined, { year: "numeric", month: "long", day: "numeric" })}</p><p className="mt-3 text-xs text-[var(--text-muted)]">Changing this date updates your study priority curve.</p></div><CalendarDays size={20} className="text-[var(--study)]" /></div>{editing ? <div className="mt-4 flex flex-wrap items-center gap-3"><input className="input w-48" type="date" value={examDate} onChange={(event) => setExamDate(event.target.value)} /><button className="btn btn-primary" onClick={() => void saveExamDate()} disabled={saving}><Save size={15} /> Save date</button><button className="btn btn-ghost" onClick={() => setEditing(false)}>Cancel</button></div> : <button className="btn btn-ghost btn-sm mt-4" onClick={() => setEditing(true)}>Edit exam date</button>}</section><section className="card"><h2 className="text-lg">Plan overview</h2><div className="mt-5 grid gap-5 sm:grid-cols-4"><Stat label="Plan started" value={new Date(settings.planStartDate).toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" })} /><Stat label="Days elapsed" value={String(elapsed)} /><Stat label="Total XP" value={settings.totalXp.toLocaleString()} /><Stat label="Current level" value={String(settings.level)} /></div></section><section className="card"><h2 className="text-lg">Nutrition targets</h2><div className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4"><Stat label="Calories" value={`${settings.targetCalories} kcal`} /><Stat label="Protein" value={`${settings.targetProtein}g`} /><Stat label="Carbs" value={`${settings.targetCarbs}g`} /><Stat label="Fat" value={`${settings.targetFat}g`} /></div><Link className="btn btn-ghost btn-sm mt-5" href="/meals">Edit these from the Nutrition page</Link></section><section className="card border-[rgba(239,68,68,0.25)]"><div className="flex items-start gap-3"><ShieldAlert size={20} className="mt-0.5 text-[var(--danger)]" /><div><h2 className="text-lg">Danger zone</h2><p className="mt-2 text-sm">This permanently deletes all your logged progress. Seeded content (habits, books, lessons) is kept.</p><div className="mt-5 flex flex-col gap-3 sm:flex-row"><input className="input max-w-xs" value={confirmation} onChange={(event) => setConfirmation(event.target.value)} placeholder="Type RESET to confirm" aria-label="Type RESET to confirm" /><button className="btn btn-danger" onClick={() => void reset()} disabled={confirmation !== "RESET" || saving}><AlertTriangle size={15} /> Reset all data</button></div></div></div></section>{message && <p className="text-sm font-semibold text-[var(--success)]">{message}</p>}</div></div>;
}

function Stat({ label, value }: { label: string; value: string }) { return <div className="stat-block"><span className="stat-label">{label}</span><span className="stat-value text-base">{value}</span></div>; }
